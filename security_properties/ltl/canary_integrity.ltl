[]($exists_stack x: has_canary(x)$  -> ($exists_stack x: (has_canary(x) && (byte(stack(x), 23) = Critical && 
                                            byte(stack(x), 16) = Critical && 
                                            byte(stack(x), 17) = Critical && 
                                            byte(stack(x), 18) = Critical && 
                                            byte(stack(x), 19) = Critical && 
                                            byte(stack(x), 20) = Critical && 
                                            byte(stack(x), 21) = Critical && 
                                            byte(stack(x), 22) = Critical))$))